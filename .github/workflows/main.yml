on:
  workflow_dispatch:
    inputs:
      vartest:
        description: 'testing variable'
        required: false
        default: 'Hello World'
        type: string
jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "${{ inputs.vartest }}!" > scripts/new_file.txt
          bash scripts/myscript.sh
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "test"
          git push
